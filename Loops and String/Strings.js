//String
let str ="Hello Wolrd";
console.log(str[0]);
//Template Literals

let sentence = `This is a template literal`
//Strings methods
/*
* str.toUpperCase()
* str.toLowerCase()
* srt.trim() // removes whitespaces
* str.slice(slice,end?)
*str.concat(str2)
*str.replace(searchVal, newVal)
*str.charAt(idx)
*/
str=str.toUpperCase()
console.log(str);
console.log(str.slice(1,5));
