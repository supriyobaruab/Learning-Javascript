//Arrays
let students = ["Shaown","Aongkon","Eemong"];/*
console.log(students[1]);
students[1]="Goru"; // Mutable array
console.log(students[1]);
*/
//Looping over an array

/*for (let i=0; i<students.length; i++){
    console.log(students[i]);
}
for (let j of students){
    console.log(j.toUpperCase());
}*/

//Array methods 
/* 
*push(): //add to end
*pop(): // delete from end & return
*toString(): // converts array to string
*concat(): // joins arrays and return new array
*unshift();// same as Push but from starting
*shift(); // same as pop but from the starting
slice(); // cut a slice from array, and return a new array
splice(); // same but it changes the original array // take e values(start, removed, replace);

//practice
tudents.push("Debongshi");
console.log(students);
let deletedvalue =students.pop();
console.log(students);
console.log("removed student is",deletedvalue);
*/
